# ========================================
# 烛龙绘影 环境变量配置（可选）
# ========================================
# 
# 说明：
# 1. API密钥推荐在应用界面中配置（设置 -> API密钥）
# 2. 以下环境变量都是可选的，仅在需要自定义时使用
# 3. 应用会优先使用界面配置，环境变量作为备用
#

# 阿里云DashScope API密钥（可选，推荐在界面配置）
# DASHSCOPE_API_KEY=your_api_key

# 上传文件夹（可选，默认: ~/.drawloong/uploads）
# UPLOAD_FOLDER=/custom/path/uploads

# 输出文件夹（可选，默认: ~/.drawloong/downloads）
# OUTPUT_FOLDER=/custom/path/downloads

# 最大文件大小（可选，默认: 10485760 bytes = 10MB）
# MAX_FILE_SIZE=10485760

# 任务数据文件（可选，默认: ~/.drawloong/tasks.json）
# 注意：现在任务主要保存在各个项目目录中
# TASKS_FILE=/custom/path/tasks.json
