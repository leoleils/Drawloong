# ========================================
# 烛龙绘影 环境变量配置（已弃用）
# ========================================
# 
# 重要说明：
# 从 v1.17.1 开始，推荐使用应用内"设置"界面配置 API 密钥
# 环境变量配置已弃用，仅保留用于向后兼容
#
# 迁移说明：
# 1. 如果此文件中有 DASHSCOPE_API_KEY，应用启动时会自动迁移到系统配置
# 2. 迁移完成后，可以删除此文件中的 API 密钥
# 3. 系统配置使用加密存储，比文件配置更安全
#

# 阿里云DashScope API密钥（已弃用，请使用应用内设置）
# DASHSCOPE_API_KEY=your_api_key

# 注意：
# - 推荐在应用的"设置"界面配置 API 密钥
# - 系统配置路径：
#   Windows: HKEY_CURRENT_USER\Software\WanX\ImageToVideo
#   macOS: ~/Library/Preferences/com.WanX.ImageToVideo.plist
#   Linux: ~/.config/WanX/ImageToVideo.conf
